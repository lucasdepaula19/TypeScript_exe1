import { Block } from "./Block";
import { blockType } from "./blockType";

export class DocumentX implements Block {
  add(xpto: blockType) {
    return xpto;
  }

  remove(xpto: blockType) {
    return xpto;
  }
}
